---
phase: 02-observability-rate-limiting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/logging_config.py
  - delivery/main.py
  - delivery/fetcher.py
  - delivery/ai.py
  - delivery/scheduler.py
  - api/webhook.py
  - bot/router.py
  - bot/commands/addtheme.py
  - bot/commands/payments.py
  - tests/test_fetcher.py
  - tests/test_webhook.py
autonomous: true
requirements:
  - OBS-01
  - OBS-03

must_haves:
  truths:
    - "All production print() calls replaced with logger calls"
    - "Every production module uses logger = logging.getLogger(__name__) at module level"
    - "Log output uses configured format with timestamp, level, and logger name"
    - "Broken feed URLs appear in logs with URL and exception detail via named logger"
  artifacts:
    - path: "bot/logging_config.py"
      provides: "Centralized logging.basicConfig() setup"
      exports: ["setup"]
    - path: "delivery/main.py"
      provides: "Logger-based output replacing all 11 print() calls"
      contains: "logger = logging.getLogger(__name__)"
    - path: "api/webhook.py"
      provides: "Logger-based error handling replacing print()"
      contains: "logger = logging.getLogger(__name__)"
    - path: "delivery/fetcher.py"
      provides: "Named logger for feed warnings"
      contains: "logger = logging.getLogger(__name__)"
  key_links:
    - from: "api/webhook.py"
      to: "bot/logging_config.py"
      via: "setup_logging() call after load_dotenv()"
      pattern: "from bot.logging_config import setup"
    - from: "delivery/main.py"
      to: "bot/logging_config.py"
      via: "setup_logging() call after load_dotenv()"
      pattern: "from bot.logging_config import setup"
    - from: "tests/test_fetcher.py"
      to: "delivery/fetcher.py"
      via: "patching named logger instead of module-level logging"
      pattern: "delivery.fetcher.logger"
---

<objective>
Create centralized logging configuration, migrate all production print() calls to named loggers, fix inline imports, and update test patches.

Purpose: Establish structured logging foundation (OBS-01) so production issues are diagnosable. Fix named logger references in tests so they keep passing after migration. Ensure broken feed logging uses named logger (OBS-03).
Output: bot/logging_config.py (new), 8 production files updated, 2 test files updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-observability-rate-limiting/02-CONTEXT.md
@.planning/phases/02-observability-rate-limiting/02-RESEARCH.md

@bot/validation.py
@delivery/main.py
@delivery/fetcher.py
@delivery/ai.py
@delivery/scheduler.py
@api/webhook.py
@bot/router.py
@bot/commands/addtheme.py
@bot/commands/payments.py
@tests/test_fetcher.py
@tests/test_webhook.py

<interfaces>
<!-- Pattern to follow (from bot/validation.py): -->
From bot/validation.py:
```python
"""URL validation utilities for RSS feed safety."""
# Module-level docstring, then imports, then exports
```
This is the established shared utility module pattern in bot/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create logging config module and migrate all modules to named loggers</name>
  <files>bot/logging_config.py, delivery/main.py, delivery/fetcher.py, delivery/ai.py, delivery/scheduler.py, api/webhook.py, bot/router.py, bot/commands/addtheme.py, bot/commands/payments.py</files>
  <read_first>
    - delivery/main.py (11 print() calls to convert; see current state before modifying)
    - api/webhook.py (1 print() + module-level logging.warning on line 18; must handle import-time logging pitfall)
    - bot/router.py (inline import logging on line 70; rate limit integration comes in plan 03)
    - delivery/fetcher.py (uses logging.warning directly; needs logger = logging.getLogger(__name__))
    - delivery/ai.py (uses logging.warning directly on line 99; needs named logger)
    - delivery/scheduler.py (uses logging.warning on lines 97, 105; needs named logger)
    - bot/commands/payments.py (uses logging.error directly; needs named logger)
    - bot/commands/addtheme.py (inline import logging on line 202; needs module-level logger)
    - bot/validation.py (reference for shared module pattern in bot/)
  </read_first>
  <action>
1. Create `bot/logging_config.py` (per D-01, D-02, D-04):
```python
"""Centralized logging configuration."""
import logging

_configured = False

def setup():
    """Configure root logger. Safe to call multiple times."""
    global _configured
    if _configured:
        return
    logging.basicConfig(
        level=logging.INFO,
        format="%(asctime)s %(levelname)s %(name)s -- %(message)s",
    )
    _configured = True
```

2. Update `api/webhook.py` (per D-01, D-03, D-05):
   - Add after line 13 (`load_dotenv()`): `from bot.logging_config import setup as setup_logging` then `setup_logging()` -- this MUST come BEFORE the `if not os.environ.get("WEBHOOK_SECRET")` check on line 17 (Pitfall 3 from RESEARCH.md).
   - Add `logger = logging.getLogger(__name__)` after imports.
   - Change line 18 `logging.warning(...)` to `logger.warning(...)`.
   - Change line 38 `print(f"[webhook] error: {e}")` to `logger.error("Webhook handler error: %s", e)`.

3. Update `delivery/main.py` (per D-03, D-05):
   - Add `import logging` and `logger = logging.getLogger(__name__)` after existing imports.
   - Add `from bot.logging_config import setup as setup_logging` right after `load_dotenv()` on line 11, then call `setup_logging()`.
   - Replace all 11 print() calls with logger equivalents:
     - Line 52: `print(f"[deliver] {date_str} Q{quarter}...")` -> `logger.info("run start: date=%s quarter=Q%d hour=%d weekday=%d", date_str, quarter, hour_utc, weekday)`
     - Line 57: `print("[deliver] No users due this hour.")` -> `logger.info("No users due this hour")`
     - Line 63: `print(f"[deliver] {len(groups)} unique theme(s)...")` -> `logger.info("%d unique theme(s) to process for %d delivery row(s)", len(groups), len(deliveries))`
     - Line 76: `print(f"[deliver] Theme ({theme_type}, {theme_id}) not found...")` -> `logger.warning("Theme not found: theme_type=%s theme_id=%d", theme_type, theme_id)`
     - Line 89: `print(f"[deliver] No new articles...")` -> `logger.info("No new articles for %s", theme["name"])`
     - Line 93: `print(f"[deliver] AI returned no summaries...")` -> `logger.info("AI returned no summaries for %s", theme["name"])`
     - Line 96: `print(f"[deliver] Error fetching/summarizing...")` -> `logger.error("Error fetching/summarizing %s: %s", theme["name"], e)`
     - Line 122: `print(f"[deliver] Failed to post to user...")` -> `logger.error("Failed to post to user %d: %s", user["user_id"], e)`
     - Line 156: `print(f"[deliver] Failed to write history...")` -> `logger.error("Failed to write digest history for theme (%s, %d): %s", theme_type, theme_id, e)`
     - Line 160: `print("[deliver] Done.")` -> `logger.info("Delivery run complete")` (this will be enhanced to run summary in plan 02)

4. Update `delivery/fetcher.py` (per D-03, D-10):
   - Add `logger = logging.getLogger(__name__)` after `import logging` on line 1.
   - Change line 24 `logging.warning(...)` to `logger.warning(...)`.
   - Change line 41 `logging.warning(...)` to `logger.warning(...)`.

5. Update `delivery/ai.py` (per D-03):
   - Add `logger = logging.getLogger(__name__)` after `import logging` on line 2.
   - Change line 99 `logging.warning(...)` to `logger.warning(...)`.

6. Update `delivery/scheduler.py` (per D-03):
   - Add `logger = logging.getLogger(__name__)` after `import logging` on line 3.
   - Change line 97 `logging.warning(...)` to `logger.warning(...)`.
   - Change line 105 `logging.warning(...)` to `logger.warning(...)`.

7. Update `bot/router.py` (per D-03, D-06):
   - Add `import logging` and `logger = logging.getLogger(__name__)` at module level (after existing imports, around line 5).
   - Remove the inline `import logging` on line 70.
   - Change line 71 `logging.warning(...)` to `logger.warning(...)`.

8. Update `bot/commands/addtheme.py` (per D-03):
   - Add `import logging` and `logger = logging.getLogger(__name__)` at module level (after existing imports).
   - Remove the inline `import logging` on line 202.
   - Change line 203 `logging.warning(...)` to `logger.warning(...)`.

9. Update `bot/commands/payments.py` (per D-03):
   - Add `logger = logging.getLogger(__name__)` after `import logging` on line 2.
   - Change all `logging.error(...)` calls to `logger.error(...)`.
  </action>
  <verify>
    <automated>cd C:/Users/TopAide/projects/telegram-news-bot && python -c "from bot.logging_config import setup; setup(); print('logging config OK')" && python -c "import ast, sys; [ast.parse(open(f).read()) for f in ['bot/logging_config.py','delivery/main.py','delivery/fetcher.py','delivery/ai.py','delivery/scheduler.py','api/webhook.py','bot/router.py','bot/commands/addtheme.py','bot/commands/payments.py']]; print('all files parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - bot/logging_config.py exists and contains `def setup():`
    - bot/logging_config.py contains `%(asctime)s %(levelname)s %(name)s -- %(message)s`
    - bot/logging_config.py contains `level=logging.INFO`
    - delivery/main.py contains `logger = logging.getLogger(__name__)` at module level
    - delivery/main.py contains `from bot.logging_config import setup as setup_logging`
    - delivery/main.py has zero `print(` calls (grep returns no matches)
    - api/webhook.py contains `logger = logging.getLogger(__name__)` at module level
    - api/webhook.py contains `from bot.logging_config import setup as setup_logging`
    - api/webhook.py contains `setup_logging()` BEFORE the `if not os.environ.get("WEBHOOK_SECRET")` line
    - api/webhook.py has zero `print(` calls
    - delivery/fetcher.py contains `logger = logging.getLogger(__name__)`
    - delivery/fetcher.py contains `logger.warning` (not bare `logging.warning`)
    - delivery/ai.py contains `logger = logging.getLogger(__name__)`
    - delivery/ai.py contains `logger.warning` (not bare `logging.warning`)
    - delivery/scheduler.py contains `logger = logging.getLogger(__name__)`
    - delivery/scheduler.py contains `logger.warning` (not bare `logging.warning`)
    - bot/router.py contains `logger = logging.getLogger(__name__)` at module level
    - bot/router.py does NOT contain `import logging` inside a function body
    - bot/commands/addtheme.py contains `logger = logging.getLogger(__name__)` at module level
    - bot/commands/addtheme.py does NOT contain inline `import logging` inside a function body
    - bot/commands/payments.py contains `logger = logging.getLogger(__name__)`
  </acceptance_criteria>
  <done>All production modules use named loggers via logging.getLogger(__name__). No print() calls remain in production code. Centralized logging config exists and is called by both entry points before any logging occurs.</done>
</task>

<task type="auto">
  <name>Task 2: Update test patches for named loggers</name>
  <files>tests/test_fetcher.py, tests/test_webhook.py</files>
  <read_first>
    - tests/test_fetcher.py (lines 79, 91 patch `delivery.fetcher.logging.warning` -- must change to `delivery.fetcher.logger`)
    - tests/test_webhook.py (line 65 patches `api.webhook.logging.warning` -- must change to `api.webhook.logger`)
    - delivery/fetcher.py (confirm logger attribute name after Task 1)
    - api/webhook.py (confirm logger attribute name after Task 1)
  </read_first>
  <action>
1. Update `tests/test_fetcher.py` (Pitfall 5 from RESEARCH.md):
   - Line 79: Change `@patch("delivery.fetcher.logging.warning")` to `@patch("delivery.fetcher.logger")`
   - Line 91: Change `@patch("delivery.fetcher.logging.warning")` to `@patch("delivery.fetcher.logger")`
   - In `test_fetch_articles_skips_restricted_url` (line 83): parameter name changes from `mock_warning` to `mock_logger`. Assertion changes from `mock_warning.called` to `mock_logger.warning.called`.
   - In `test_fetch_articles_logs_broken_feed_with_url` (line 94): parameter name changes from `mock_warning` to `mock_logger`. Update assertions:
     - `mock_warning.called` -> `mock_logger.warning.called`
     - `mock_warning.call_args` -> `mock_logger.warning.call_args`
     - The log_msg format assertion stays the same (extracting args from call_args and formatting).

2. Update `tests/test_webhook.py`:
   - Line 65: Change `@patch("api.webhook.logging.warning")` to `@patch("api.webhook.logger")`
   - In `test_webhook_logs_warning_when_secret_absent` (line 67): parameter name changes from `mock_warning` to `mock_logger`. Update assertions:
     - `mock_warning.called` -> `mock_logger.warning.called`
     - `mock_warning.call_args` -> `mock_logger.warning.call_args`
     - `mock_warning.call_args[0][0]` -> `mock_logger.warning.call_args[0][0]`
  </action>
  <verify>
    <automated>cd C:/Users/TopAide/projects/telegram-news-bot && python -m pytest tests/test_fetcher.py tests/test_webhook.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_fetcher.py does NOT contain `delivery.fetcher.logging.warning`
    - tests/test_fetcher.py contains `delivery.fetcher.logger`
    - tests/test_webhook.py does NOT contain `api.webhook.logging.warning`
    - tests/test_webhook.py contains `api.webhook.logger`
    - `python -m pytest tests/test_fetcher.py tests/test_webhook.py -x` exits with code 0
  </acceptance_criteria>
  <done>All test patches target named logger instances instead of module-level logging. All existing tests pass.</done>
</task>

</tasks>

<verification>
- `grep -r "^[^#]*print(" delivery/main.py api/webhook.py` returns zero matches (no print() in production)
- `grep -rn "logging.getLogger(__name__)" delivery/main.py delivery/fetcher.py delivery/ai.py delivery/scheduler.py api/webhook.py bot/router.py bot/commands/addtheme.py bot/commands/payments.py` returns one match per file (8 total)
- `python -m pytest tests/ -x` passes all tests
- `python -c "from bot.logging_config import setup; setup()"` runs without error
</verification>

<success_criteria>
- Zero print() calls in delivery/main.py and api/webhook.py
- 8 production modules have module-level `logger = logging.getLogger(__name__)`
- bot/logging_config.py exists with setup() function
- Both entry points (api/webhook.py, delivery/main.py) call setup_logging() at startup
- All tests pass with updated logger patches
</success_criteria>

<output>
After completion, create `.planning/phases/02-observability-rate-limiting/02-01-SUMMARY.md`
</output>
